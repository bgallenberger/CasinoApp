<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CasinoApp.Views.Play.PlayPage"
             Title="{Binding FullName}">

    <StackLayout Margin="20" VerticalOptions="StartAndExpand">
        <Label Text="{Binding BannerText}" />
        <Label Text="Enter your Win amount" />
        <Entry Text="{Binding Win}" />
        <Button Text="Add Win" Clicked="OnAddClicked" />
        <Button Text="Add Bonus" Clicked="OnAddJackpotClicked" />
        <Button Text="Cancel" Clicked="OnCancelClicked" />
    </StackLayout>
</ContentPage>